<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="ltd.newbee.mall.newbeemall.dao.ECGoodsCategoryMapper">
	<select id="selectGoodsCategory"
		resultType="ltd.newbee.mall.newbeemall.entity.ECGoodsCategory">
		SELECT a.*,c.img
		FROM ec_goods_category as a
		left join ec_goods_category_img as c
		on a.category_id=c.category_id 
		where a.active=0;
	</select>
</mapper>