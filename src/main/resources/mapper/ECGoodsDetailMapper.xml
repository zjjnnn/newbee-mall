<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="ltd.newbee.mall.newbeemall.dao.ECGoodsDetailMapper">
	<select id="selectGoodsDetail"
		resultType="ltd.newbee.mall.newbeemall.entity.TbNewbeeMallGoodsInfo">
		SELECT info.goods_id,info.goods_name,info.goods_cover_img,info.selling_price
		FROM config
		JOIN tb_newbee_mall_goods_info AS info
		ON info.goods_id = config.goods_id
		WHERE config.config_type=#{configType}
	</select>
</mapper>